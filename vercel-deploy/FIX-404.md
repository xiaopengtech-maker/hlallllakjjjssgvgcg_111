# 🔥 FIX LỖI 404 - HƯỚNG DẪN NHANH

## ⚠️ VẤN ĐỀ

Vercel vẫn chạy build script cũ từ cache. Cần cấu hình trực tiếp trên Dashboard.

## ✅ GIẢI PHÁP - LÀM THEO TỪNG BƯỚC

### Bước 1: Vào Vercel Dashboard

1. Truy cập: https://vercel.com
2. Chọn project: **viettel-nnnananana**
3. Click vào tab **"Settings"**

### Bước 2: Cấu hình Build & Development Settings

1. Trong Settings, tìm mục **"Build & Development Settings"**
2. Cấu hình như sau:

```
Framework Preset: Other
├─ Build Command: [LEAVE EMPTY - Để trống]
├─ Output Directory: [LEAVE EMPTY - Để trống]  
├─ Install Command: [LEAVE EMPTY - Để trống]
└─ Development Command: [LEAVE EMPTY - Để trống]
```

**QUAN TRỌNG:** Tất cả đều để trống hoặc chọn "Override" và xóa hết!

### Bước 3: Cấu hình Root Directory

1. Vẫn trong Settings → General
2. Tìm **"Root Directory"**
3. Click **"Edit"**
4. Chọn: **`vercel-deploy`**
5. Click **"Save"**

### Bước 4: Xóa Cache và Redeploy

1. Vào tab **"Deployments"**
2. Click vào deployment mới nhất
3. Click nút **"..."** (3 chấm)
4. Chọn **"Redeploy"**
5. **QUAN TRỌNG:** Bỏ tick **"Use existing Build Cache"**
6. Click **"Redeploy"**

## 🎯 KIỂM TRA

Sau khi redeploy xong:
- Truy cập: `https://viettel-nnnananana.vercel.app`
- Phải thấy giao diện "Tạo Link Thanh Toán MoMo"
- Click nút test

## 🔍 NẾU VẪN LỖI

### Kiểm tra Build Logs:

1. Vào Deployments
2. Click vào deployment mới nhất
3. Xem logs
4. Phải thấy: **"No build command specified"** hoặc tương tự
5. KHÔNG được thấy: "Running npm run build"

### Nếu vẫn thấy "Running npm run build":

**Nguyên nhân:** Settings chưa được save đúng

**Giải pháp:**
1. Quay lại Settings
2. Build Command: Click "Override" → Xóa hết → Save
3. Output Directory: Click "Override" → Xóa hết → Save
4. Redeploy lại (nhớ bỏ tick cache)

## 📝 CHECKLIST

- [ ] Root Directory = `vercel-deploy`
- [ ] Framework Preset = Other
- [ ] Build Command = EMPTY (trống)
- [ ] Output Directory = EMPTY (trống)
- [ ] Install Command = EMPTY (trống)
- [ ] Redeploy WITHOUT cache

## 💡 TẠI SAO BỊ LỖI?

Vercel tự động detect `package.json` và chạy `npm run build`.
Nhưng build script cũ vẫn còn trong cache.
Phải override settings trên Dashboard để tắt hoàn toàn build process.

## 🆘 VẪN KHÔNG ĐƯỢC?

Thử cách này:
1. Xóa project trên Vercel
2. Tạo project mới
3. Import lại từ GitHub
4. Lần này nhớ cấu hình đúng ngay từ đầu:
   - Root Directory: `vercel-deploy`
   - Framework: Other
   - Build Command: Để trống
   - Output Directory: Để trống

---

**LƯU Ý:** Đây là static site + serverless functions, KHÔNG CẦN build process!
